#ifndef UTILS_H
#define UTILS_H

void loadMNISTData(const char* trainFile, const char* testFile,
    double **trainingImages, double **trainingLabels,
    double **testImages, double **testLabels );

#endif
